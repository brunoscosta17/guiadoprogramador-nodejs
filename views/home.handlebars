<h1>Lista de posts:</h1>

{{#each posts}}
    <h1>{{ title }}</h1>
    <p>{{ content }}</p>
    <small>{{ createdAt }}</small>
    <a href="/delete/{{id}}"><button>Delete</button></a>    
    <hr>
{{/each}}